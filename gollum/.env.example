# Gollum Environment Variables
# Copy this file to .env and update the values
#
# Gollum is a simple wiki system built on top of Git
# See: https://github.com/gollum/gollum

# ============================================================================
# Web Server Settings
# ============================================================================
GOLLUM_PORT=8170
# Web interface port
# Default: 8170 (changed from 4567 to avoid port conflicts)
# Change if port 8170 is already in use

# ============================================================================
# Gollum Configuration
# ============================================================================
GOLLUM_WIKI_PATH=/wiki
# Path to wiki content inside container
# This is the Git repository where pages are stored

# ============================================================================
# Container Configuration
# ============================================================================
GOLLUM_CONTAINER_NAME=gollum
DOLLUM_CONTAINER_NAME=gollum-init
# dollum is a helper container for initialization

# ============================================================================
# Volume Configuration
# ============================================================================
GOLLUM_VOLUME=gollum-wiki-data
# Volume for wiki content

# ============================================================================
# USAGE NOTES
# ============================================================================
# Gollum is a Git-powered wiki with a simple interface.
#
# Features:
# - Git-backed storage (every edit is a commit)
# - Markdown, RDoc, Org-mode support
# - Full-text search
# - File attachments
# - Edit history via Git
#
# After starting:
# 1. Access Gollum: http://localhost:8310
# 2. Create your first page (e.g., "Home")
# 3. All changes are automatically committed to Git
#
# Advanced usage:
# - Clone wiki repository:
#   docker compose exec gollum git clone /wiki /tmp/wiki-backup
#
# - View Git history:
#   docker compose exec gollum git -C /wiki log
#
# - Restore from backup:
#   docker compose cp wiki-backup gollum:/tmp/
#   docker compose exec gollum git -C /wiki pull /tmp/wiki-backup
#
# Supported markup formats:
# - Markdown (.md, .markdown)
# - RDoc (.rdoc)
# - Org-mode (.org)
# - Textile (.textile)
# - Pod (.pod)
# - MediaWiki (.mediawiki, .wiki)
