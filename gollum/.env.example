# Gollum Environment Configuration
# Copy this file to .env and customize the values

# ====================
# Project Settings
# ====================
PROJECT_NAME=gollum
TZ=Asia/Seoul

# ====================
# Port Configuration
# ====================
# Gollum web port
GOLLUM_PORT=8310

# ====================
# User Configuration
# ====================
# User name for file ownership
USER01=user01

# User ID (match with host user for file permissions)
UID=1000

# Group ID
GID=1000

# ====================
# Git Configuration
# ====================
# Git author name (for commits)
GIT_AUTHOR_NAME=Wiki Author

# Git author email
GIT_AUTHOR_EMAIL=wiki@example.com

# Git committer name
GIT_COMMITTER_NAME=Wiki Author

# Git committer email
GIT_COMMITTER_EMAIL=wiki@example.com

# ====================
# Gollum Configuration
# ====================
# Wiki directory (container path)
WIKI_PATH=/wiki

# Allow editing (true/false)
# GOLLUM_ALLOW_EDITING=true

# Default page format (markdown, asciidoc, org, textile, rdoc, pod, mediawiki, creole, rst)
# GOLLUM_DEFAULT_FORMAT=markdown

# Page file extension
# GOLLUM_PAGE_FILE_DIR=pages

# ====================
# Authentication (Optional)
# ====================
# Enable authentication
# GOLLUM_AUTH=basic

# Basic auth username
# GOLLUM_AUTH_USER=admin

# Basic auth password
# GOLLUM_AUTH_PASSWORD=change-me-password

# ====================
# Search Configuration (Optional)
# ====================
# Enable search
# GOLLUM_SEARCH=true

# ====================
# Container Names
# ====================
GOLLUM_CONTAINER_NAME=gollum_dev
DOLLUM_CONTAINER_NAME=gollum_dollum

# ====================
# Volume Names
# ====================
GOLLUM_WIKI_VOLUME=gollum

# ====================
# Network Names
# ====================
# NETWORK_NAME=gollum-network

# ====================
# Security Note
# ====================
# ⚠️ Security recommendations:
# - Enable authentication for public-facing wikis
# - Use HTTPS with reverse proxy
# - Regular Git repository backups
# - Configure proper file permissions
# - Restrict access with firewall rules
# - Regular security updates
#
# Authentication setup:
# - Basic auth can be enabled via Gollum options
# - Or use reverse proxy (nginx, Traefik) for auth
#
# Initial setup:
# 1. Copy this file to .env and update values
# 2. Start services: docker compose up -d
# 3. Access: http://localhost:8310
# 4. Create your first page:
#    - Click "New Page"
#    - Enter page name
#    - Write content in Markdown
#    - Click "Save"
#
# Wiki backup:
# The wiki is stored as a Git repository in the volume.
# Backup options:
#
# 1. Export Git repository:
#    docker run --rm -v gollum_gollum:/wiki -v $(pwd):/backup alpine tar czf /backup/wiki-backup.tar.gz /wiki
#
# 2. Clone repository:
#    docker compose exec gollum git clone file:///wiki /tmp/wiki-export
#    docker cp gollum_dev:/tmp/wiki-export ./wiki-backup
#
# 3. Push to remote Git repository:
#    docker compose exec gollum bash
#    # Inside container:
#    cd /wiki
#    git remote add backup https://github.com/username/wiki-backup.git
#    git push backup master
#
# Supported markup formats:
# - Markdown (.md, .markdown)
# - AsciiDoc (.asciidoc, .ad, .adoc)
# - Org-mode (.org)
# - Textile (.textile)
# - RDoc (.rdoc)
# - Pod (.pod)
# - MediaWiki (.mediawiki, .wiki)
# - Creole (.creole)
# - reStructuredText (.rst)
#
# Git operations:
# All wiki edits are automatically committed to Git.
# View history:
# - Click on any page
# - Click "History" button
# - See all revisions and diffs
#
# Advanced Git operations:
# docker compose exec gollum bash
# # Inside container:
# cd /wiki
# git log
# git diff HEAD~1
# git revert <commit-hash>
#
# Customize appearance:
# - Custom CSS: Create custom.css in wiki directory
# - Custom JavaScript: Create custom.js
# - Templates: Modify Gollum templates
#
# Integration with external Git:
# 1. Initialize with existing repository:
#    docker compose down
#    docker run --rm -v gollum_gollum:/wiki alpine sh -c "rm -rf /wiki/* /wiki/.*"
#    docker run --rm -v gollum_gollum:/wiki alpine git clone https://github.com/user/repo.git /wiki
#    docker compose up -d
#
# 2. Sync with remote:
#    docker compose exec gollum bash
#    cd /wiki
#    git pull origin master
#    git push origin master
#
# References:
# - Gollum documentation: https://github.com/gollum/gollum
# - Gollum wiki: https://github.com/gollum/gollum/wiki
# - Port guide: ../docs/PORT_GUIDE.md
