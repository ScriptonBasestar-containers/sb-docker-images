# Bitcoin Core Environment Variables
# Copy this file to .env and update the values
#
# Bitcoin Core node with optional Block Explorer
# See: https://bitcoin.org/

# ============================================================================
# Port Configuration
# ============================================================================
BITCOIND_RPC_PORT=8332
# Bitcoin RPC port
# Default: 8332

BITCOIND_P2P_PORT=8333
# Bitcoin P2P network port
# Default: 8333

BTC_EXPLORER_PORT=3002
# BTC RPC Explorer web interface port
# Default: 3002

# ============================================================================
# Bitcoin RPC Configuration
# ============================================================================
RPC_USER=bitcoin
# RPC username
# Default: bitcoin

RPC_PASSWORD=changeme
# RPC password
# WARNING: Change this to a strong password!
# Generate with: openssl rand -base64 32

# ============================================================================
# Network Configuration
# ============================================================================
TESTNET=0
# Network mode
# 0 = Mainnet (default)
# 1 = Testnet
# WARNING: Mainnet will download the full blockchain (~500GB+)

# ============================================================================
# Node Configuration
# ============================================================================
PRUNE=0
# Blockchain pruning
# 0 = Disabled (full node, recommended)
# 550 = Minimum prune size in MB
# Higher values = more blockchain history retained

# ============================================================================
# Container Names
# ============================================================================
BITCOIND_CONTAINER_NAME=bitcoind
BTC_EXPLORER_CONTAINER_NAME=btc-explorer

# ============================================================================
# USAGE NOTES
# ============================================================================
# Bitcoin Core setup:
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Configure your settings:
#    - Set strong RPC_PASSWORD
#    - Choose network (mainnet/testnet)
#    - Configure pruning if disk space is limited
#
# 3. Start Bitcoin node:
#    docker compose up -d bitcoind
#
# 4. (Optional) Start Block Explorer:
#    docker compose up -d btc-rpc-explorer
#
# 5. Access services:
#    - Bitcoin RPC: localhost:8332
#    - P2P Network: localhost:8333
#    - Block Explorer: http://localhost:3002
#
# RPC Usage:
#    docker compose exec bitcoind bitcoin-cli -rpcuser=bitcoin -rpcpassword=changeme getblockchaininfo
#
# IMPORTANT NOTES:
# ============================================================================
# - Mainnet blockchain download takes days and requires 500GB+ disk space
# - Testnet is recommended for development and testing
# - Initial sync requires significant CPU and bandwidth
# - Use pruning if disk space is limited (not recommended for full node)
# - Always use strong RPC password in production
# - Consider firewall rules for P2P port (8333)
#
# TESTNET NOTES:
# - Testnet blockchain is much smaller (~30GB)
# - Faster initial sync
# - Uses different ports (18332 RPC, 18333 P2P)
# - Free testnet coins available from faucets
#
# SECURITY:
# - Never expose RPC port to the internet
# - Use strong passwords
# - Consider VPN or SSH tunnel for remote access
# - Keep software updated
# ============================================================================
