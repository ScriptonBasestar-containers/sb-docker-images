# Jenkins Environment Variables
# Copy this file to .env and update the values
#
# Jenkins is an open-source automation server for CI/CD
# See: https://www.jenkins.io/

# ============================================================================
# Jenkins Port Configuration
# ============================================================================
JENKINS_HTTP_PORT=8080
# Web interface port (host mapping)
# Default: 8080 (matches compose settings; use 8600 if following PORT_GUIDE)
# Change if the port is already in use

JENKINS_AGENT_PORT=50000
# Port for Jenkins agents (JNLP)
# Default: 50000

# ============================================================================
# System Configuration
# ============================================================================
TZ=Asia/Seoul
# Timezone setting
# Options: Asia/Seoul, UTC, America/New_York, Europe/London, etc.

# ============================================================================
# Container Configuration
# ============================================================================
JENKINS_CONTAINER_NAME=jenkins
# Container name

# ============================================================================
# Java Options (Optional)
# ============================================================================
# JAVA_OPTS=-Xmx2g -Xms1g
# JVM heap size settings
# Adjust based on your needs:
# - Small: -Xmx512m -Xms256m
# - Medium: -Xmx1g -Xms512m
# - Large: -Xmx2g -Xms1g

# ============================================================================
# USAGE NOTES
# ============================================================================
# Initial Setup:
# 1. Start Jenkins:
#    docker compose up -d
#
# 2. Get initial admin password:
#    docker compose exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
#
# 3. Access Jenkins:
#    http://localhost:8080
#
# 4. Complete setup wizard:
#    - Enter initial admin password
#    - Install suggested plugins
#    - Create admin user
#
# Docker Socket (Optional):
# To use Docker inside Jenkins, uncomment the docker.sock volume in compose.yml
# WARNING: This gives Jenkins full Docker access on the host
#
# Backup:
#   docker compose exec jenkins tar -czf /tmp/jenkins-backup.tar.gz /var/jenkins_home
#   docker compose cp jenkins:/tmp/jenkins-backup.tar.gz ./jenkins-backup.tar.gz
#
# Restore:
#   docker compose cp jenkins-backup.tar.gz jenkins:/tmp/
#   docker compose exec jenkins tar -xzf /tmp/jenkins-backup.tar.gz -C /
