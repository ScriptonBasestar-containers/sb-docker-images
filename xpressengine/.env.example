# XpressEngine 3 환경 변수 설정
# 이 파일을 .env로 복사하여 값을 수정하세요

# ====================
# 프로젝트 설정
# ====================
PROJECT_NAME=xpressengine
TZ=Asia/Seoul

# ====================
# 포트 설정
# ====================
# XpressEngine 웹 포트
XE_PORT=8220

# MariaDB 포트 (선택사항)
MARIADB_PORT=3306

# Redis 포트 (선택사항)
REDIS_PORT=6379

# ====================
# Laravel 애플리케이션 설정
# ====================
# 환경 (local, development, production)
APP_ENV=production

# 애플리케이션 이름
APP_NAME=XE Website

# 디버그 모드 (프로덕션에서는 false!)
APP_DEBUG=false

# 사이트 URL
APP_URL=http://localhost:8220

# 타임존
APP_TIMEZONE=Asia/Seoul

# 로그 설정 (single, daily, syslog, errorlog)
APP_LOG=daily
APP_LOG_LEVEL=info

# ====================
# 데이터베이스 설정 (MariaDB)
# ====================
# 데이터베이스 연결 타입 (sqlite, mysql, pgsql)
DB_CONNECTION=mysql

# 데이터베이스 호스트
DB_HOST=mariadb

# 데이터베이스 포트
DB_PORT=3306

# 데이터베이스 이름
DB_DATABASE=xe_db

# 데이터베이스 사용자
DB_USERNAME=root

# 데이터베이스 비밀번호 (프로덕션에서는 반드시 변경!)
DB_PASSWORD=change-me-strong-password

# 테이블 접두사 (선택사항)
DB_PREFIX=

# MariaDB 설정
MYSQL_ROOT_PASSWORD=change-me-root-password
MYSQL_DATABASE=xe_db
MYSQL_USER=xe_user
MYSQL_PASSWORD=change-me-strong-password

# ====================
# 캐시 설정
# ====================
# 캐시 드라이버 (file, redis, memcached)
CACHE_DRIVER=redis

# ====================
# Redis 설정
# ====================
# Redis 호스트
REDIS_HOST=redis

# Redis 포트
REDIS_PORT=6379

# Redis 비밀번호 (프로덕션에서는 반드시 설정!)
REDIS_PASSWORD=change-me-redis-password

# ====================
# 세션 설정
# ====================
# 세션 드라이버 (file, cookie, database, redis)
SESSION_DRIVER=redis

# ====================
# 큐 설정
# ====================
# 큐 드라이버 (sync, database, redis)
QUEUE_DRIVER=redis

# ====================
# 파일 시스템 설정
# ====================
# 파일 시스템 드라이버 (local, s3, rackspace, ftp)
FILESYSTEM_DRIVER=local

# ====================
# 메일 설정 (선택사항)
# ====================
# 메일 드라이버 (smtp, sendmail, mailgun, mandrill, ses, log)
# MAIL_DRIVER=smtp
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=your-username
# MAIL_PASSWORD=your-password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=noreply@example.com
# MAIL_FROM_NAME=XE Website

# ====================
# 컨테이너 이름
# ====================
XE_CONTAINER_NAME=xpressengine
MARIADB_CONTAINER_NAME=xe-mariadb
REDIS_CONTAINER_NAME=xe-redis

# ====================
# 볼륨 이름
# ====================
MARIADB_DATA_VOLUME=xe-mariadb-data
REDIS_DATA_VOLUME=xe-redis-data
XE_STORAGE_VOLUME=xe-storage

# ====================
# 네트워크 이름
# ====================
NETWORK_NAME=xe-network

# ====================
# 보안 경고
# ====================
# ⚠️ 중요: 프로덕션 사용 전 모든 비밀번호와 시크릿을 변경하세요!
# - DB_PASSWORD: 데이터베이스 비밀번호
# - MYSQL_ROOT_PASSWORD: 데이터베이스 관리자 비밀번호
# - REDIS_PASSWORD: Redis 비밀번호
# - APP_DEBUG: 프로덕션에서는 반드시 false로 설정
#
# ⚠️ XE3 지원 중단 경고:
# XpressEngine 3는 2020년 이후 업데이트가 중단되었습니다.
# - 보안 취약점이 발견될 수 있습니다
# - 프로덕션 사용을 권장하지 않습니다
# - WordPress, Drupal, Joomla 등의 대안을 고려하세요
# - 테스트 및 학습 목적으로만 사용하는 것이 좋습니다
#
# 보안 권장사항:
# - 강력한 비밀번호 사용
# - HTTPS 활성화 (리버스 프록시 사용)
# - APP_DEBUG=false 설정 (프로덕션)
# - 정기적인 보안 업데이트 (가능한 경우)
# - 방화벽 설정
# - 백업 구성
# - 로그 모니터링
#
# 비밀번호 생성 방법:
# openssl rand -base64 32
#
# 초기 설정:
# 1. XE3 소스 코드 다운로드: make prepare
# 2. Docker 이미지 빌드: make docker-build-debian
# 3. 이 파일을 .env로 복사
# 4. 모든 비밀번호 변경
# 5. docker compose up -d 실행
# 6. http://localhost:8220 접속
# 7. 설치 마법사로 초기 설정 완료
#
# 데이터베이스 설정 (설치 마법사에서):
# - DB Host: mariadb (localhost 아님!)
# - DB Name: xe_db
# - DB User: root
# - DB Password: 위에서 설정한 비밀번호
# - DB Prefix: (비워둠)
#
# 관리자 페이지:
# URL: http://localhost:8220/settings
#
# 권장 대안 프로젝트:
# - WordPress: 가장 인기 있는 CMS
# - Drupal: 엔터프라이즈급 CMS
# - Joomla: 유연한 CMS
# - October CMS: Laravel 기반 CMS
#
# 참고 자료:
# - XE3 GitHub: https://github.com/xpressengine/xpressengine
# - 포트 가이드: ../docs/PORT_GUIDE.md
