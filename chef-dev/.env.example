# Chef Development Environment Variables
# Copy this file to .env and update the values
#
# Chef development environment for infrastructure as code
# See: https://www.chef.io/

# ============================================================================
# User Configuration
# ============================================================================
CUSTOM_USER=devuser
# Username for the development container
# This user will have sudo privileges

# ============================================================================
# Chef Version
# ============================================================================
CHEF_VERSION=3.4.28
# Chef Client/Workstation version
# Check available versions: https://downloads.chef.io/

# ============================================================================
# Docker Image Configuration
# ============================================================================
DOCKER_REPONAME=archmagece
# Docker repository name

DOCKER_IMGNAME=chef-dev
# Docker image name

DOCKER_IMGVERSION=${CHEF_VERSION}
# Docker image version tag (uses CHEF_VERSION)

# ============================================================================
# USAGE NOTES
# ============================================================================
# This is a development environment for Chef infrastructure automation.
#
# Build the image:
#   ./build.sh
#
# Run the container:
#   docker run -it --rm ${DOCKER_REPONAME}/${DOCKER_IMGNAME}:${DOCKER_IMGVERSION}
#
# Mount your Chef repository:
#   docker run -it --rm -v $(pwd)/chef-repo:/chef-repo ${DOCKER_REPONAME}/${DOCKER_IMGNAME}:${DOCKER_IMGVERSION}
