# Django CMS Environment Variables
# Copy this file to .env and update the values
#
# Django CMS is an enterprise-grade content management system
# See: https://www.django-cms.org/

# ============================================================================
# Web Server Settings
# ============================================================================
DJANGO_CMS_WEB_PORT=8000
# Default: 8000 (recommended port for Django CMS backend)
# Change this if you need to avoid port conflicts

DJANGO_CMS_FRONTEND_PORT=8090
# Frontend development server port

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
POSTGRES_DB=db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
# For development, PostgreSQL trust authentication is used
# For production, set a strong password!
POSTGRES_HOST_AUTH_METHOD=trust

DB_PORT=5432
# PostgreSQL port (exposed)

# ============================================================================
# Django Settings
# ============================================================================
SECRET_KEY=change-this-to-random-50-character-string
# Django SECRET_KEY - MUST be changed in production!
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

DEBUG=False
# Set to False in production

ALLOWED_HOSTS=localhost,127.0.0.1
# Comma-separated list of allowed hosts

# ============================================================================
# Django CMS Settings
# ============================================================================
CMS_LANGUAGES_MAIN=en
# Main language code (en, ko, de, fr, etc.)

# ============================================================================
# Container Names
# ============================================================================
DJANGO_CMS_WEB_CONTAINER=django-cms-web
DJANGO_CMS_FRONTEND_CONTAINER=django-cms-frontend
POSTGRES_CONTAINER_NAME=django-cms-db

# ============================================================================
# Volume Names
# ============================================================================
DJANGO_CMS_APP_VOLUME=django-cms-app
DJANGO_CMS_DATA_VOLUME=django-cms-data
DJANGO_CMS_FRONTEND_VOLUME=django-cms-frontend

# ============================================================================
# SECURITY WARNINGS
# ============================================================================
# 🔒 IMPORTANT: Before deploying to production:
#
# 1. Change SECRET_KEY to a strong random value (50+ chars)
# 2. Set DEBUG to False
# 3. Set proper PostgreSQL password (POSTGRES_PASSWORD)
# 4. Remove POSTGRES_HOST_AUTH_METHOD=trust
# 5. Update ALLOWED_HOSTS with your actual domain
# 6. Set up SSL/TLS certificates (HTTPS)
# 7. Configure proper static/media file serving
# 8. Set up email backend (SMTP)
# 9. Review Django security checklist
# 10. Configure proper backup strategy
#
# Documentation:
# https://docs.django-cms.org/
# https://docs.djangoproject.com/en/stable/howto/deployment/checklist/
# ============================================================================
