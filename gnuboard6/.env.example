# GNUboard6 환경 변수 설정
# 이 파일을 .env로 복사하여 값을 수정하세요

# ====================
# 프로젝트 설정
# ====================
PROJECT_NAME=gnuboard6
TZ=Asia/Seoul

# ====================
# 포트 설정
# ====================
# 웹 서버 포트
WEB_PORT=8080

# ====================
# 데이터베이스 설정 (MariaDB)
# ====================
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=gnuboard6
MYSQL_USER=gnuboard
MYSQL_PASSWORD=passw0rd
MYSQL_HOST=mariadb
MYSQL_PORT=3306

# ====================
# Django 설정
# ====================
# 데이터베이스 연결 정보
DB_ENGINE=django.db.backends.mysql
DB_NAME=gnuboard6
DB_USER=gnuboard
DB_PASSWORD=passw0rd
DB_HOST=mariadb
DB_PORT=3306

# Django 시크릿 키 (프로덕션에서는 반드시 변경!)
SECRET_KEY=change-me-in-production

# 디버그 모드 (프로덕션에서는 false로 설정)
DEBUG=true

# 허용된 호스트 (쉼표로 구분)
ALLOWED_HOSTS=localhost,127.0.0.1

# ====================
# 컨테이너 이름
# ====================
GNUBOARD6_CONTAINER_NAME=gnuboard6
MARIADB_CONTAINER_NAME=gnuboard6-mariadb

# ====================
# 볼륨 이름
# ====================
GNUBOARD6_MEDIA_VOLUME=gnuboard6-media
GNUBOARD6_STATIC_VOLUME=gnuboard6-static
MARIADB_DATA_VOLUME=mariadb-data

# ====================
# 네트워크 이름
# ====================
APP_NETWORK_NAME=app-network
DB_NETWORK_NAME=db-network

# ====================
# 보안 경고
# ====================
# ⚠️ 중요: 프로덕션 사용 전 모든 비밀번호와 설정을 변경하세요!
# - MYSQL_ROOT_PASSWORD: 데이터베이스 관리자 비밀번호
# - MYSQL_PASSWORD: 데이터베이스 사용자 비밀번호
# - SECRET_KEY: Django 시크릿 키 (랜덤 문자열로 변경)
# - DEBUG: 프로덕션에서는 반드시 false로 설정
# - ALLOWED_HOSTS: 실제 도메인으로 변경
#
# Django 시크릿 키 생성 방법:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
