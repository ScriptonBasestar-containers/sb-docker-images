# Misago Environment Variables
# Copy this file to .env and update the values
#
# Misago is a modern forum application written in Python and Django
# See: https://misago-project.org/

# ============================================================================
# Web Server Settings
# ============================================================================
MISAGO_PORT=8260
# Default: 8260 (recommended port for Misago)
# Change this if you need to avoid port conflicts

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
MISAGO_POSTGRES_HOST=postgres
MISAGO_POSTGRES_DB=db01
MISAGO_POSTGRES_USER=user01
MISAGO_POSTGRES_PASSWORD=passw0rd
# WARNING: Change these credentials in production!
# Use strong, unique passwords (16+ characters)

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================================================
# Django Settings
# ============================================================================
SECRET_KEY=change-me-to-random-50-character-string
# Django SECRET_KEY - MUST be changed in production!
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

DEBUG=False
# NEVER set to True in production!

ALLOWED_HOSTS=localhost,127.0.0.1
# Comma-separated list of allowed hosts
# For production, add your domain (e.g., forum.example.com)

# ============================================================================
# Site Configuration
# ============================================================================
VIRTUAL_HOST=localhost
# Your domain name for nginx-proxy

VIRTUAL_PORT=8000
# Internal application port

VIRTUAL_PROTO=uwsgi
# Protocol for nginx-proxy communication

# ============================================================================
# SSL/TLS Configuration
# ============================================================================
SSL_POLICY=Mozilla-Modern
# SSL policy for nginx-proxy
# Options: Mozilla-Modern, Mozilla-Intermediate, Mozilla-Old

ENABLE_IPV6=true
# Enable IPv6 support

# ============================================================================
# Email Settings (SMTP)
# ============================================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@example.com
# Configure your SMTP settings for email notifications

# ============================================================================
# Container Names
# ============================================================================
MISAGO_CONTAINER_NAME=misago
POSTGRES_CONTAINER_NAME=misago-postgres
REDIS_CONTAINER_NAME=misago-redis
CELERY_WORKER_CONTAINER=misago-celery-worker
NGINX_PROXY_CONTAINER=misago-nginx-proxy

# ============================================================================
# Volume Names
# ============================================================================
POSTGRES_DATA_VOLUME=misago-postgres-15
REDIS_DATA_VOLUME=misago-redis-6
NGINX_CERTS_VOLUME=nginx-certs
NGINX_HTML_VOLUME=nginx-html

# ============================================================================
# SECURITY WARNINGS
# ============================================================================
# 🔒 IMPORTANT: Before deploying to production:
#
# 1. Change SECRET_KEY to a strong random value (50+ chars)
# 2. Set DEBUG to False
# 3. Change all database passwords
# 4. Update ALLOWED_HOSTS with your actual domain
# 5. Configure proper SMTP settings
# 6. Set up SSL/TLS certificates via Let's Encrypt
# 7. Review and configure backup strategy
# 8. Set up rate limiting and DDoS protection
# 9. Configure Celery for background tasks properly
# 10. Review Django security checklist
#
# Documentation:
# https://misago-project.org/
# https://docs.djangoproject.com/en/stable/howto/deployment/checklist/
# ============================================================================
