# openNAMU Standalone Environment Variables
# Copy this file to .env and update the values
#
# openNAMU는 the seed 엔진 기반의 한국어 위키 소프트웨어입니다
# See: https://github.com/opennamu/opennamu

# ============================================================================
# 웹 서버 설정
# ============================================================================
OPENNAMU_PORT=8330
# 기본값: 8330 (openNAMU 권장 포트)
# 포트 충돌을 피하려면 이 값을 변경하세요

# ============================================================================
# 언어 설정
# ============================================================================
LANGUAGE=ko_KR
# 위키 언어 설정
# 옵션: ko_KR, en_US, ja_JP 등

# ============================================================================
# 데이터베이스 설정 (MariaDB)
# ============================================================================
DB_TYPE=mysql
# 데이터베이스 타입
# 옵션: mysql, sqlite (standalone 구성은 mysql 권장)

DB_HOST=mariadb
DB_PORT=3306
DB_NAME=opennamu
DB_USER=opennamu
DB_PASSWORD=password
# 경고: 프로덕션 환경에서는 반드시 비밀번호를 변경하세요!
# 보안을 위해 강력한 비밀번호를 사용하세요 (16자 이상)

# MariaDB Root 설정
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=opennamu
MYSQL_USER=opennamu
MYSQL_PASSWORD=password
# WARNING: Change these passwords in production!

# ============================================================================
# 관리자 설정
# ============================================================================
ADMIN_PASSWORD=admin123
# 초기 관리자 비밀번호
# 경고: 프로덕션에서는 강력한 비밀번호로 변경하세요!
# 최초 설정 후 웹 인터페이스에서 변경 가능

# ============================================================================
# 위키 설정
# ============================================================================
WIKI_NAME=openNAMU Wiki
# 위키 이름

WIKI_SKIN=default
# 위키 스킨/테마
# 옵션: default, namu, buma 등

# ============================================================================
# 컨테이너 이름
# ============================================================================
OPENNAMU_CONTAINER_NAME=opennamu
MARIADB_CONTAINER_NAME=opennamu-mariadb

# ============================================================================
# 볼륨 이름
# ============================================================================
OPENNAMU_DATA_VOLUME=opennamu-data
OPENNAMU_UPLOAD_VOLUME=opennamu-uploads
MARIADB_DATA_VOLUME=mariadb-data

# ============================================================================
# 설치 가이드
# ============================================================================
# 1. .env 파일 생성 및 비밀번호 변경
#    cp .env.example .env
#    vi .env  # 비밀번호 수정
#
# 2. 서비스 시작
#    docker compose up -d
#
# 3. 브라우저로 접속
#    http://localhost:8330
#
# 4. 초기 설정
#    - 관리자 계정 생성 (또는 ADMIN_PASSWORD 사용)
#    - 위키 이름 설정
#    - 기본 설정 완료
#
# 5. 첫 문서 작성
#    - FrontPage 또는 대문 작성
#    - the seed 문법 사용

# ============================================================================
# 보안 경고
# ============================================================================
# 🔒 중요: 프로덕션 배포 전 확인사항:
#
# 1. 모든 비밀번호 변경 (DB, Admin)
# 2. 강력하고 고유한 비밀번호 사용 (16자 이상)
# 3. SSL/TLS 인증서 설정 (HTTPS)
# 4. 정기 백업 전략 수립
# 5. 파일 업로드 제한 설정
# 6. ACL(접근 제어) 설정 검토
# 7. 스팸 방지 설정
# 8. IP 차단 목록 관리
# 9. 관리자 페이지 접근 제한
# 10. 정기적인 데이터베이스 백업
#
# openNAMU 문서:
# https://github.com/opennamu/opennamu
# https://github.com/opennamu/opennamu/wiki
# ============================================================================
