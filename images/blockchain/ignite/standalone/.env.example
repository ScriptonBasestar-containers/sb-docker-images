# Apache Ignite Standalone Environment Configuration
# Copy this file to .env and customize the values

# ====================
# Project Settings
# ====================
PROJECT_NAME=ignite
TZ=Asia/Seoul

# ====================
# Port Configuration
# ====================
# Thin client port (JDBC, ODBC, .NET, C++, Python, Node.js)
IGNITE_THIN_CLIENT_PORT=10800

# REST API port
IGNITE_REST_PORT=11211

# Discovery port (for cluster nodes)
IGNITE_DISCOVERY_PORT=47100

# Communication port (for cluster nodes)
IGNITE_COMMUNICATION_PORT=47500

# ====================
# Docker Image
# ====================
# Available tags: latest, 2.16.0, 2.15.0
IGNITE_TAG=latest

# ====================
# Ignite Configuration
# ====================
# Optional libraries (comma-separated)
# Available: ignite-rest-http, ignite-spring, ignite-indexing, etc.
IGNITE_OPTION_LIBS=ignite-rest-http

# ====================
# JVM Configuration
# ====================
# Minimum heap size
IGNITE_JVM_MIN_MEMORY=512m

# Maximum heap size (adjust based on your needs)
IGNITE_JVM_MAX_MEMORY=512m

# ====================
# Container Names
# ====================
IGNITE_CONTAINER_NAME=ignite

# ====================
# Network Names
# ====================
IGNITE_NETWORK=ignite-network

# ====================
# Volume Names
# ====================
IGNITE_PERSISTENCE_VOLUME=ignite-persistence
IGNITE_WORK_VOLUME=ignite-work

# ====================
# Security Notes
# ====================
# ⚠️  IMPORTANT Security Recommendations:
#
# 1. Network Security:
#    - Do NOT expose Ignite ports to public internet
#    - Use firewall to restrict access
#    - Bind to localhost in production
#
# 2. Authentication & Authorization:
#    - Enable Ignite authentication
#    - Configure user credentials
#    - Use SSL/TLS for connections
#
# 3. Resource Limits:
#    - Set appropriate JVM memory based on data size
#    - Monitor memory usage
#    - Configure off-heap memory
#
# 4. Persistence:
#    - Enable native persistence for durability
#    - Regular backups of persistence volume
#    - Test restore procedures
#
# 5. Production Deployment:
#    - Use cluster mode (multiple nodes)
#    - Configure proper discovery mechanism
#    - Set up monitoring and alerting

# ====================
# JVM Memory Recommendations
# ====================
# Small dataset (< 1GB):
#   IGNITE_JVM_MIN_MEMORY=512m
#   IGNITE_JVM_MAX_MEMORY=512m
#
# Medium dataset (1-10GB):
#   IGNITE_JVM_MIN_MEMORY=1g
#   IGNITE_JVM_MAX_MEMORY=2g
#
# Large dataset (> 10GB):
#   IGNITE_JVM_MIN_MEMORY=2g
#   IGNITE_JVM_MAX_MEMORY=4g
#
# Note: Leave ~25% of system RAM for OS

# ====================
# Usage Examples
# ====================
# Start Ignite:
#   make up
#
# Check version:
#   make version
#
# Access shell:
#   make shell
#
# REST API examples:
#   # Get version
#   curl http://localhost:11211/ignite?cmd=version
#
#   # Get topology
#   curl http://localhost:11211/ignite?cmd=top
#
#   # Put data to cache
#   curl "http://localhost:11211/ignite?cmd=put&cacheName=myCache&key=1&val=Hello"
#
#   # Get data from cache
#   curl "http://localhost:11211/ignite?cmd=get&cacheName=myCache&key=1"

# ====================
# Ignite Features
# ====================
# Apache Ignite provides:
# - Distributed Cache (key-value store)
# - Distributed SQL Database
# - In-Memory Data Grid
# - Compute Grid (distributed computing)
# - Service Grid (distributed services)
# - Streaming & CEP (Complex Event Processing)
# - Machine Learning
# - Apache Spark Integration
#
# Use Cases:
# - High-performance caching
# - Real-time analytics
# - HTAP (Hybrid Transactional/Analytical Processing)
# - Distributed computing
# - IoT data processing

# ====================
# Cluster Configuration
# ====================
# For production, use multiple Ignite nodes:
#
# 1. Create custom config file (config/ignite-config.xml)
# 2. Mount it in compose.yml:
#    volumes:
#      - ./config/ignite-config.xml:/opt/ignite/apache-ignite/config/ignite-config.xml
# 3. Start with config:
#    IGNITE_CONFIG_FILE=/opt/ignite/apache-ignite/config/ignite-config.xml
#
# 4. Scale to multiple nodes:
#    docker compose up -d --scale ignite=3

# ====================
# References
# ====================
# - Apache Ignite: https://ignite.apache.org/
# - Documentation: https://ignite.apache.org/docs/latest/
# - REST API: https://ignite.apache.org/docs/latest/restapi
# - Thin Clients: https://ignite.apache.org/docs/latest/thin-clients/getting-started-with-thin-clients
# - Docker Hub: https://hub.docker.com/r/apacheignite/ignite
# - Port guide: ../../docs/PORT_STATUS.md
