# DevPI Environment Variables
# Copy this file to .env and update the values
#
# DevPI is a Python package index server and PyPI cache
# See: https://devpi.net/

# ============================================================================
# Build Arguments
# ============================================================================
INSTALL_WEB=true
# Install devpi-web for web interface
# Options: true, false

INSTALL_CONSTRAINED=false
# Install with version constraints
# Options: true, false

INSTALL_FINDLINKS=false
# Install devpi-findlinks plugin
# Options: true, false

INSTALL_JENKINS=false
# Install devpi-jenkins plugin for Jenkins integration
# Options: true, false

INSTALL_LOCKDOWN=false
# Install devpi-lockdown plugin for security
# Options: true, false

# ============================================================================
# DevPI Server Configuration
# ============================================================================
DEVPI_HOST=0.0.0.0
# Host address to bind
# Default: 0.0.0.0 (all interfaces)

DEVPI_PORT=3141
# Port for DevPI server
# Default: 3141

# DEVPI_WEB_THEME=semantic-ui
# Web interface theme
# Options: semantic-ui, bootstrap, etc.

# ============================================================================
# Container Configuration
# ============================================================================
DEVPI_CONTAINER_NAME=devpi-server
# Container name

# ============================================================================
# Port Configuration
# ============================================================================
DEVPI_EXTERNAL_PORT=3141
# External port to expose
# Change if port 3141 is already in use

# ============================================================================
# USAGE NOTES
# ============================================================================
# DevPI Server provides:
# - Private Python package index
# - PyPI caching proxy
# - Package testing and staging
# - Multi-index support
#
# After starting:
# 1. Access web interface: http://localhost:3141
# 2. Initialize devpi:
#    docker compose exec devpi devpi-init
# 3. Create user:
#    docker compose exec devpi devpi user -c <username> password=<password>
# 4. Create index:
#    docker compose exec devpi devpi index -c <username>/<indexname>
#
# Upload packages:
#   pip install devpi-client
#   devpi use http://localhost:3141
#   devpi login <username>
#   devpi upload
#
# Install from devpi:
#   pip install --index-url http://localhost:3141/<username>/<indexname>/+simple/ <package>
