# Gitea Environment Variables
# Copy this file to .env and update the values
#
# Gitea is a painless self-hosted Git service
# See: https://docs.gitea.io/

# ============================================================================
# Port Configuration
# ============================================================================
GITEA_HTTP_PORT=3001
# Gitea web interface port
# Default: 3001
# Access Gitea at http://localhost:3001

GITEA_SSH_PORT=2222
# Gitea SSH port for git operations
# Default: 2222
# Use: git clone ssh://git@localhost:2222/user/repo.git

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_DB=gitea
# PostgreSQL database name
# Default: gitea

POSTGRES_USER=gitea
# PostgreSQL username
# Default: gitea

POSTGRES_PASSWORD=gitea
# PostgreSQL password
# Default: gitea
# WARNING: Change in production!

# ============================================================================
# User Configuration
# ============================================================================
USER_UID=1000
# User ID for file permissions
# Default: 1000
# Should match your host user ID

USER_GID=1000
# Group ID for file permissions
# Default: 1000
# Should match your host group ID

# ============================================================================
# Container Names
# ============================================================================
GITEA_CONTAINER_NAME=gitea
GITEA_DB_CONTAINER_NAME=gitea-db

# ============================================================================
# USAGE NOTES
# ============================================================================
# Setup:
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update configuration:
#    - Change POSTGRES_PASSWORD
#    - Adjust USER_UID/USER_GID if needed
#    - Modify ports if conflicting
#
# 3. Start services:
#    docker compose up -d
#
# 4. Access Gitea:
#    http://localhost:3001
#
# 5. Complete initial setup wizard:
#    - Database type: PostgreSQL
#    - Host: db:5432
#    - Database: gitea
#    - Username: gitea
#    - Password: (from POSTGRES_PASSWORD)
#    - SSH Server Domain: localhost
#    - SSH Server Port: 2222
#    - HTTP Port: 3000 (internal)
#    - Base URL: http://localhost:3001
#    - Create admin account
#
# Git Operations:
# HTTP: git clone http://localhost:3001/user/repo.git
# SSH:  git clone ssh://git@localhost:2222/user/repo.git
#
# SSH Configuration (~/.ssh/config):
#   Host gitea
#     HostName localhost
#     Port 2222
#     User git
#
# Then use: git clone gitea:user/repo.git
#
# PRODUCTION NOTES:
# ============================================================================
# For production deployment:
# 1. Change POSTGRES_PASSWORD to strong password
# 2. Use proper domain name in Base URL
# 3. Set up SSL/TLS (reverse proxy recommended)
# 4. Configure email settings (SMTP)
# 5. Enable 2FA for admin accounts
# 6. Set up regular backups (data/ and postgres/)
# 7. Review security settings
# 8. Configure webhook secrets
# 9. Set up monitoring
# 10. Consider using external PostgreSQL for HA
# ============================================================================
