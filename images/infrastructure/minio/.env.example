# MinIO Environment Variables
# Copy this file to .env and update the values
#
# MinIO is a high-performance object storage compatible with Amazon S3 API
# See: https://min.io/

# ============================================================================
# Port Configuration
# ============================================================================
MINIO_PORT=9000
# MinIO S3 API port
# Default: 9000
# Used for S3-compatible API access

MINIO_CONSOLE_PORT=9001
# MinIO Web Console port
# Default: 9001
# Used for web-based administration

# ============================================================================
# Authentication
# ============================================================================
MINIO_ROOT_USER=minioadmin
# MinIO root username
# Default: minioadmin
# WARNING: Change in production!

MINIO_ROOT_PASSWORD=minioadmin
# MinIO root password
# Default: minioadmin
# WARNING: Must be at least 8 characters. Change in production!

# ============================================================================
# Bucket Configuration (Optional)
# ============================================================================
# Default buckets created by minio-setup service:
# - backups: For backup storage
# - media: For media files (public download)
# - uploads: For user uploads
#
# Modify compose.yml to add/remove buckets

# ============================================================================
# Container Names
# ============================================================================
MINIO_CONTAINER_NAME=minio
MINIO_SETUP_CONTAINER_NAME=minio-setup

# ============================================================================
# USAGE NOTES
# ============================================================================
# Setup:
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update credentials:
#    - Set strong MINIO_ROOT_USER
#    - Set strong MINIO_ROOT_PASSWORD (min 8 characters)
#
# 3. Start services:
#    docker compose up -d
#
# 4. Access MinIO Console:
#    http://localhost:9001
#    Login with MINIO_ROOT_USER and MINIO_ROOT_PASSWORD
#
# 5. S3 API endpoint:
#    http://localhost:9000
#
# S3 Client Configuration:
# - Endpoint: http://localhost:9000
# - Access Key: MINIO_ROOT_USER
# - Secret Key: MINIO_ROOT_PASSWORD
# - Region: us-east-1 (default)
#
# AWS CLI example:
#   aws configure set aws_access_key_id minioadmin
#   aws configure set aws_secret_access_key minioadmin
#   aws --endpoint-url http://localhost:9000 s3 ls
#
# Python boto3 example:
#   s3 = boto3.client('s3',
#       endpoint_url='http://localhost:9000',
#       aws_access_key_id='minioadmin',
#       aws_secret_access_key='minioadmin')
#
# PRODUCTION NOTES:
# ============================================================================
# For production deployment:
# 1. Use strong credentials (min 8 characters for password)
# 2. Enable TLS/SSL (reverse proxy recommended)
# 3. Configure proper access policies
# 4. Set up regular backups
# 5. Review security best practices
# 6. Consider distributed mode for HA
# 7. Monitor disk space usage
# ============================================================================
