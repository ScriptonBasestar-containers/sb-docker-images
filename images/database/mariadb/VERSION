# Project Version
# Format: MAJOR.MINOR.PATCH (semantic versioning)
# Git tag format: mariadb-vMAJOR.MINOR.PATCH
VERSION=2.0.0

# Version history (most recent first)
# v2.0.0 - 2026-05-30 - Server + backup restructure
#   - Add compose.yml for standalone MariaDB server (was missing — Makefile pointed at nothing)
#   - Move backup tooling into backup/ subdirectory
#   - Backup containers: alpine 3.14 → 3.20 (EOL fix), restic 0.15.3 → 0.16.4 from apk
#   - Fix backup-rclone.sh: --incremental-basedir= empty value crash, auto-detect prior snapshot
#   - Write backup-restic.sh (was 0 bytes — never worked)
#   - Unify .env.example to cover server + backup
# v1.0.0 - 2025-11-24 - Initial release
