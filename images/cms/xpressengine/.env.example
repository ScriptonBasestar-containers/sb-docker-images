# XpressEngine Environment Variables
# Copy this file to .env and update the values
#
# XpressEngine은 한국형 CMS 플랫폼입니다
# See: https://www.xpressengine.io/

# ============================================================================
# 웹 서버 설정
# ============================================================================
XE_PORT=8270
# 기본값: 8270 (changed from 8080 to avoid port conflicts)
# 포트 충돌을 피하려면 이 값을 변경하세요

# ============================================================================
# Laravel/XE 애플리케이션 설정
# ============================================================================
APP_ENV=production
# Options: production, local, development
# 프로덕션에서는 반드시 production으로 설정

APP_NAME=XE Website
# 애플리케이션 이름

APP_DEBUG=false
# 프로덕션에서는 반드시 false로 설정

APP_URL=http://localhost:8270
# 애플리케이션 URL (프로덕션에서는 실제 도메인 사용)

APP_TIMEZONE=Asia/Seoul
# 타임존 설정

APP_LOG=daily
# 로그 타입: single, daily, syslog, errorlog

APP_LOG_LEVEL=info
# 로그 레벨: debug, info, notice, warning, error, critical, alert, emergency

# ============================================================================
# 데이터베이스 설정 (MySQL/MariaDB)
# ============================================================================
DB_CONNECTION=mysql
DB_HOST=mariadb
DB_PORT=3306
DB_DATABASE=xe_db
DB_USERNAME=root
DB_PASSWORD=password
DB_PREFIX=
# 경고: 프로덕션 환경에서는 반드시 비밀번호를 변경하세요!

# MariaDB 설정
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=xe_db
# WARNING: Change these in production!

# ============================================================================
# 캐시 설정
# ============================================================================
CACHE_DRIVER=redis
# Options: file, redis, memcached
# 프로덕션에서는 redis 또는 memcached 권장

# Redis 설정
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=password
# WARNING: Change password in production!

# Memcached 설정 (선택사항)
# MEMCACHED_PERSISTENT_ID=
# MEMCACHED_HOST=127.0.0.1
# MEMCACHED_PORT=11211

# ============================================================================
# 세션 설정
# ============================================================================
SESSION_DRIVER=redis
# Options: file, cookie, database, apc, memcached, redis, array

# ============================================================================
# 큐 설정
# ============================================================================
QUEUE_DRIVER=redis
# Options: sync, database, beanstalkd, sqs, redis, null

# ============================================================================
# 파일 시스템 설정
# ============================================================================
FILESYSTEM_DRIVER=local
# Options: local, s3, rackspace, ftp

# AWS S3 설정 (선택사항)
# FILESYSTEM_CLOUD=s3
# AWS_KEY=
# AWS_SECRET=
# AWS_REGION=
# AWS_BUCKET=

# ============================================================================
# 메일 설정 (선택사항)
# ============================================================================
# MAIL_DRIVER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_FROM_ADDRESS=noreply@example.com
# MAIL_FROM_NAME=XE Website
# MAIL_ENCRYPTION=tls
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Mailgun 설정
# MAILGUN_DOMAIN=
# MAILGUN_SECRET=

# ============================================================================
# CAPTCHA 설정 (선택사항)
# ============================================================================
# CAPTCHA_DRIVER=google
# CAPTCHA_APIS_GOOGLE_SITEKEY=
# CAPTCHA_APIS_GOOGLE_SECRET=
# CAPTCHA_APIS_NAVER_CLIENT_ID=
# CAPTCHA_APIS_NAVER_SECRET=

# ============================================================================
# 컨테이너 이름
# ============================================================================
XE_CONTAINER_NAME=xe
REDIS_CONTAINER_NAME=xe-redis
MARIADB_CONTAINER_NAME=xe-mariadb

# ============================================================================
# 볼륨 이름
# ============================================================================
REDIS_DATA_VOLUME=redis-data

# ============================================================================
# 보안 경고
# ============================================================================
# 🔒 중요: 프로덕션 배포 전 확인사항:
#
# 1. APP_ENV를 production으로 설정
# 2. APP_DEBUG를 false로 설정
# 3. 모든 비밀번호 변경 (DB, Redis)
# 4. APP_URL을 실제 도메인으로 변경
# 5. SSL/TLS 인증서 설정 (HTTPS)
# 6. 메일 설정 구성 (SMTP)
# 7. 정기 백업 전략 수립
# 8. Redis/MariaDB 포트 외부 노출 제거
# 9. 파일 업로드 제한 설정
# 10. Laravel 보안 체크리스트 검토
#
# XpressEngine 문서:
# https://xpressengine.gitbook.io/
# ============================================================================
