# Django CMS Standalone Environment Variables
# Copy this file to .env and update the values
#
# Django CMS is an enterprise-grade content management system
# See: https://www.django-cms.org/

# ============================================================================
# Web Server Settings
# ============================================================================
DJANGO_CMS_WEB_PORT=8000
# Default: 8000 (recommended port for Django CMS)
# Change this if you need to avoid port conflicts

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
POSTGRES_DB=djangocms
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# WARNING: Change POSTGRES_PASSWORD in production!
# Use a strong, unique password (16+ characters)

DATABASE_URL=postgres://postgres:postgres@postgres:5432/djangocms
# Database connection string (auto-generated from above variables)

# ============================================================================
# Django Settings
# ============================================================================
SECRET_KEY=change-me-in-production-use-random-50-character-string
# Django SECRET_KEY - MUST be changed in production!
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

DEBUG=False
# Set to False in production
# NEVER set to True in production!

ALLOWED_HOSTS=localhost,127.0.0.1
# Comma-separated list of allowed hosts
# For production: add your domain (e.g., cms.example.com)

# ============================================================================
# Django CMS Settings
# ============================================================================
CMS_LANGUAGES_MAIN=en
# Main language code
# Options: en, ko, de, fr, es, it, etc.

# ============================================================================
# Container Names
# ============================================================================
DJANGO_CMS_WEB_CONTAINER=django-cms-web
POSTGRES_CONTAINER_NAME=django-cms-postgres

# ============================================================================
# Volume Names
# ============================================================================
POSTGRES_DATA_VOLUME=postgres-data
DJANGO_CMS_DATA_VOLUME=django-cms-data
DJANGO_CMS_MEDIA_VOLUME=django-cms-media
DJANGO_CMS_STATIC_VOLUME=django-cms-static

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Generate SECRET_KEY:
#    python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
#
# 2. Update POSTGRES_PASSWORD with a strong password
#
# 3. Update ALLOWED_HOSTS with your domain
#
# 4. Start services:
#    docker compose up -d
#
# 5. Run migrations:
#    docker compose exec web python manage.py migrate
#
# 6. Create superuser:
#    docker compose exec web python manage.py createsuperuser
#
# 7. Collect static files:
#    docker compose exec web python manage.py collectstatic --noinput

# ============================================================================
# SECURITY WARNINGS
# ============================================================================
# 🔒 IMPORTANT: Before deploying to production:
#
# 1. Generate unique SECRET_KEY (50+ characters)
# 2. Set DEBUG to False
# 3. Change POSTGRES_PASSWORD to a strong password
# 4. Update ALLOWED_HOSTS with your actual domain
# 5. Set up SSL/TLS certificates (HTTPS)
# 6. Configure static/media file serving (use CDN or S3)
# 7. Set up email backend (SMTP)
# 8. Configure proper backup strategy
# 9. Review Django security checklist
# 10. Set up monitoring and logging
# 11. Configure CORS if needed
# 12. Set up rate limiting
#
# Documentation:
# https://docs.django-cms.org/
# https://docs.djangoproject.com/en/stable/howto/deployment/checklist/
# ============================================================================
