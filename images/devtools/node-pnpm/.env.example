# Node.js with pnpm Environment Variables
# Copy this file to .env and update the values
#
# pnpm is a fast, disk space efficient package manager
# See: https://pnpm.io/

# ============================================================================
# Node.js Configuration
# ============================================================================
NODE_VERSION=22
# Node.js version
# Supported: 18, 20, 22 (LTS recommended)
# Check available versions: https://nodejs.org/en/about/releases/

# ============================================================================
# pnpm Configuration
# ============================================================================
PNPM_VERSION=9
# pnpm major version
# Check available versions: https://github.com/pnpm/pnpm/releases

# ============================================================================
# Application Configuration
# ============================================================================
NODE_ENV=development
# Node environment: development, production, test

# ============================================================================
# Docker Image Configuration
# ============================================================================
DOCKER_REPONAME=scriptonbasestar
# Docker repository name

# ============================================================================
# IMAGE VARIANTS
# ============================================================================
# This project provides three image variants:
#
# 1. node-pnpm:22 (Dockerfile)
#    - Debian bookworm-slim based
#    - Standard runtime image
#    - Includes: git, tini, curl
#
# 2. node-pnpm:22-alpine (Dockerfile.alpine)
#    - Alpine Linux based
#    - Smallest image size
#    - Good for production deployments
#
# 3. node-pnpm:22-builder (Dockerfile.builder)
#    - Debian bookworm based
#    - Includes: build-essential, python3
#    - Good for CI/CD pipelines
#
# ============================================================================
# USAGE EXAMPLES
# ============================================================================
#
# Build default image:
#   make build
#
# Build Alpine variant:
#   make build-alpine
#
# Build builder variant:
#   make build-builder
#
# Run interactive shell:
#   make shell
#
# Create new pnpm project:
#   docker run -it --rm -v $(pwd):/app scriptonbasestar/node-pnpm:22 pnpm init
#
# Install dependencies:
#   docker run -it --rm -v $(pwd):/app scriptonbasestar/node-pnpm:22 pnpm install
#
# Run scripts:
#   docker run -it --rm -v $(pwd):/app scriptonbasestar/node-pnpm:22 pnpm run build
#
# Use as CI base image:
#   FROM scriptonbasestar/node-pnpm:22-builder
#   WORKDIR /app
#   COPY package.json pnpm-lock.yaml ./
#   RUN pnpm install --frozen-lockfile
#   COPY . .
#   RUN pnpm build
