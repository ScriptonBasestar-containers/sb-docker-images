# Database Configuration
POSTGRES_USER=outline
POSTGRES_PASSWORD=outline
POSTGRES_DB=outline

# Application URL
URL=http://localhost:3000
PORT=3000

# Security Keys (REQUIRED - Generate with: openssl rand -hex 32)
SECRET_KEY=generate_a_random_secret_key_here
UTILS_SECRET=generate_another_random_secret_key_here

# SMTP Configuration (Optional - for email notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_SECURE=true

# Slack OAuth (Optional - for Slack login)
# Get credentials from: https://api.slack.com/apps
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
