# n8n - Workflow Automation Platform
# Copy this file to .env and update the values
#
# Official Documentation: https://docs.n8n.io/
# Docker Hub: https://hub.docker.com/r/n8nio/n8n

# ============================================================================
# Application Configuration
# ============================================================================
N8N_VERSION=latest
# n8n version (e.g., latest, 1.21.1)
# See: https://hub.docker.com/r/n8nio/n8n/tags

WEB_PORT=5678
# Web UI port
# Default: 5678

N8N_HOST=localhost
# Hostname for n8n
# Default: localhost
# Production: your-domain.com

N8N_PROTOCOL=http
# Protocol (http or https)
# Default: http
# Production: https (with reverse proxy)

WEBHOOK_URL=http://localhost:5678/
# Webhook URL for external services
# IMPORTANT: Change this in production to your actual domain
# Example: https://n8n.example.com/

# ============================================================================
# Database Configuration
# ============================================================================
DB_TYPE=sqlite
# Database type: sqlite (default) or postgresdb
# SQLite: Simple, no additional setup needed
# PostgreSQL: Better for production, multiple instances

# PostgreSQL Configuration (uncomment and configure if DB_TYPE=postgresdb)
# POSTGRES_VERSION=16-alpine
# POSTGRES_DB=n8n
# POSTGRES_USER=n8n
# POSTGRES_PASSWORD=changeme  # SECURITY: Change in production!

# ============================================================================
# Execution Configuration
# ============================================================================
EXECUTIONS_PROCESS=main
# Execution process mode
# Options: main, own
# main: Execute in main process (simple)
# own: Execute in separate process (better isolation)

EXECUTIONS_MODE=regular
# Execution mode
# Options: regular, queue
# regular: Direct execution
# queue: Use queue (requires Redis, better for high load)

# ============================================================================
# Security Configuration
# ============================================================================
N8N_BASIC_AUTH_ACTIVE=true
# Enable basic authentication
# Default: true
# IMPORTANT: Keep enabled in production!

N8N_BASIC_AUTH_USER=admin
# Basic auth username
# Default: admin
# SECURITY: Change in production!

N8N_BASIC_AUTH_PASSWORD=changeme
# Basic auth password
# Default: changeme
# SECURITY: Change to strong password in production!

# ============================================================================
# Advanced Configuration (Optional)
# ============================================================================

# Encryption Key (for credentials)
# N8N_ENCRYPTION_KEY=your-random-encryption-key-here
# Generate: openssl rand -base64 32

# Email Configuration (SMTP)
# N8N_EMAIL_MODE=smtp
# N8N_SMTP_HOST=smtp.gmail.com
# N8N_SMTP_PORT=587
# N8N_SMTP_USER=your-email@gmail.com
# N8N_SMTP_PASS=your-app-password
# N8N_SMTP_SENDER=n8n@example.com

# Logging
# N8N_LOG_LEVEL=info
# Options: error, warn, info, verbose, debug

# Timezone
# GENERIC_TIMEZONE=Asia/Seoul

# External Hooks
# EXTERNAL_HOOK_FILES=/data/hooks/my-hook.js

# Community Nodes
# N8N_COMMUNITY_PACKAGES_ENABLED=true
